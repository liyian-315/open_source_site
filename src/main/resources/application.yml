mybatis:
  mapper-locations: classpath:Mapper/*.xml
  type-aliases-package: com.sdu.open.source.site.entity
  config-location: classpath:mybatis-config.xml

server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rm-bp148z5e593e5u63nvo.mysql.rds.aliyuncs.com:3306/openSourceSite?serverTimezone=UTC
    username: root
    password: Admin!@#

  mail:
    username: cislc210sdu@163.com
    password: TTid83v3vnt7sWSi
    host: smtp.163.com
    # 端口号465或587
    port: 465
    protocol: smtps
    default-encoding: UTF-8
    # 配置SSL 加密工厂
    properties:
      mail:
        smtp:
          socketFactoryClass: javax.net.ssl.SSLSocketFactory
          #表示开启 DEBUG 模式
          debug: true
          ssl: true
